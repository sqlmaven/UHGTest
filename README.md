# UHGTest

This repository `UHGTest` was initialized locally. Create the remote on GitHub and push the initial commit.

Steps to publish:

- Create repo on GitHub (see instructions below)
- Push local `main` branch to remote
